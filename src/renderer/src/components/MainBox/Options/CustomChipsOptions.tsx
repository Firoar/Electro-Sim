import classes from './Options.module.css'
import clsx from 'clsx'

const CustomChipsOptions = () => {
  return (
    <div className={clsx(classes['option-div'], classes['custom-option-div'])}>
      CustomChipsOptions
    </div>
  )
}
export default CustomChipsOptions
